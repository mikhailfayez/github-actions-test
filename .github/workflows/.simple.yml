name: Shell commands

on: [push]

jobs: 
  run-shell-command: 
    runs-on: ubuntu-16.04
    steps: 
      - name: echo a string
        run: echo "hello world"
      - name: multiline script 
        run: |
          node -v 
          npm -v 
        